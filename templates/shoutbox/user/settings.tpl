<div class="checkbox">
    <label><input type="checkbox" data-property="shoutbox:toggles:hide" <!-- IF settings.shoutbox:toggles:hide -->checked<!-- ENDIF settings.shoutbox:toggles:hide -->> Hide shoutbox</label>
</div>
<div class="checkbox">
    <label><input type="checkbox" data-property="shoutbox:toggles:sound" <!-- IF settings.shoutbox:toggles:sound -->checked<!-- ENDIF settings.shoutbox:toggles:sound -->> Play a sound on a new shout</label>
</div>
<div class="checkbox">
    <label><input type="checkbox" data-property="shoutbox:toggles:notification" <!-- IF settings.shoutbox:toggles:notification -->checked<!-- ENDIF settings.shoutbox:toggles:notification -->> Display a notification in the titlebar on a new shout</label>
</div>